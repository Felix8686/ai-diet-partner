# 项目交接文档

## 项目当前状态

- 项目：AI Diet Partner（AI 健身饮食管理产品）。
- 当前阶段：饮食管理 MVP 原型，Next.js + TypeScript，移动端优先。
- 当前开发分支：`hermes/round-03`；禁止直接修改 `main`。
- 当前 PR：[#3 feat: connect local personalized meal planning](https://github.com/Felix8686/ai-diet-partner/pull/3)，目标 `main`，OPEN，未合并。
- PR #3 当前 head：`42f7f930451b2311f7634fde1f05bbf9af7669fc`。
- GitHub Actions CI run #16：SUCCESS。
- Round 03 已实现本地确定性餐食规划、统一 localStorage 数据层、周方案持久化、采购清单派生与勾选持久化、每日反馈读取/保存。
- 当前仍不接 Supabase、认证、真实 AI Provider 或第三方服务。

## 当前产品边界

继续严格遵守 `AGENTS.md` 与 `docs/PRODUCT.md`：

- MVP 只做饮食管理。
- 前五个大版本禁止加入吉祥物、卡通 IP、拟人角色。
- 不加入喝水、步数、久坐、运动打卡、训练计划等泛健康功能。
- 不做卡路里流水账，不做冰箱库存管理。
- 外卖、食堂、便利店、外食都是正常饮食环境，不得按“异常情况”处理。
- 避免“未达标、还差多少、预计减重”等考核式表达。
- 未经明确任务不得扩展地图、美团/高德、MCP、外部 Agent、真实 AI Provider。

## Round 03 已完成内容

- 建立结构化餐食模板池，包含餐别、场景、准备时间、厨房能力、粗略成本、主要食材、标签和替换方案。
- 实现确定性的 `generateWeekPlan(profile, week)`，让厨房条件、做饭时间、外食环境、喜恶/禁忌、预算和重复度真实影响主方案。
- 建立统一 storage 层，集中管理 profile、当前周方案、采购清单和按日期反馈，并对损坏 JSON 安全回退。
- 三步建档完成后保存 profile，生成当前周方案、派生采购清单并跳转周方案。
- 首页和周方案读取已保存方案；采购勾选刷新后保持；无方案时显示建档引导。
- 反馈页改用统一数据层。
- 首页已改为按餐别独立渲染，部分餐别没有可行模板时不会把其他餐别一起隐藏。
- 替换方案已改为从当前 profile 过滤后的合法候选模板生成；无合法候选时不显示替换入口。
- 餐食模板增加结构化 `dietaryTags`，纯素/全素过滤已覆盖当前模板池中的茶叶蛋等动物性食材表达。

## Round 03 当前验证结果

- `npm run lint`：通过（当前实际为 `tsc --noEmit`）。
- `npm test`：23 个测试通过。
- `npm run build`：通过。
- GitHub Actions：最新 head 的 CI run #16 通过。
- Hermes 已完成本地浏览器 smoke：无方案引导 → 三步建档 → 生成 7 天方案 → 首页/周方案 → 采购清单 → 刷新保持 → 每日反馈。
- 严格纯素画像、替换方案厨房/时间约束、部分餐别缺失场景已有回归验证。

## ChatGPT 第二次复验结论

前一轮指出的 3 项问题已经修正。但继续检查完整数据闭环后，仍发现以下 3 项属于 Round 03 合并前应修正的问题，因此 PR #3 暂不合并。

### 1. 采购清单把食堂 / 外卖 / 外食的“原材料”也算成用户要购买的食材

当前 `deriveShoppingList(plan)` 会无差别遍历所有餐食的 `ingredients`。

这会导致例如：

- 周一午餐 = 公司食堂“鱼 + 青菜 + 米饭”
- 周二午餐 = 外卖鸡肉饭

采购清单仍要求用户购买鱼、青菜、米饭、鸡肉等原材料。

这与产品最重要的现实场景原则冲突：用户在食堂或点外卖时，不应该为了这顿饭购买原材料。

要求：

- 采购清单必须区分“用户需要自己采购的东西”和“由食堂/餐厅/外卖提供的食物”。
- `公司食堂`、`外卖`、`外食`餐食不得把其原材料加入家庭采购清单。
- `便利店`属于用户实际购买场景，可以加入用户需要买的成品，例如茶叶蛋、玉米、无糖豆浆、即食鸡胸等，但不要把它们错误拆成无需单独购买的内部原料。
- 最好给模板增加一个很轻量的结构化字段，例如 `shoppingItems` / `procurementMode` 或等价方案，而不是继续只靠 `ingredients` 猜测；不要做大型库存系统。
- 增加自动测试：以“工作日食堂/外卖为主”的 profile 生成方案后，采购清单不能出现这些外食餐食的原材料；便利店需要购买的成品仍可出现。

### 2. 晚餐无可行模板时，晚间每日反馈入口会一起消失

当前首页 evening 分支把“晚餐卡片”和“每日反馈入口”放在同一个 `dinner ? ... : MissingMeal` 条件里。

因此如果严格禁忌/条件导致当天晚餐没有可行模板，用户只能看到“晚餐暂时没有合适模板”，却无法提交当天反馈。

每日反馈是核心闭环的一部分，不应依赖晚餐是否生成成功。

要求：

- 晚间每日反馈入口必须独立于 `dinner` 是否存在。
- 有晚餐：显示晚餐 + 反馈入口。
- 无晚餐：显示轻量缺失提示 + 反馈入口。
- 增加回归测试或浏览器 smoke 场景证明“无晚餐时仍可进入每日反馈”。

### 3. `weekdayWeekendDifference` 目前没有真正参与规划，周末总是被无条件放宽

当前 `canUseTemplate()` 在 `isWeekend` 时直接返回 `true`（厨房能力过滤之后），因此会跳过工作日准备时间和“通常不做饭”等限制；而建档中的 `weekdayWeekendDifference` 没有用于规划。

结果是：用户即使选择“每天时间差不多”或“周末反而更忙”，周末仍可能突然被安排 30 分钟的家庭烹饪餐。

要求采用简单、确定性的 v0 规则即可：

- `工作日更忙，周末有时间`：周末允许比工作日更宽松，可安排较长家庭烹饪。
- `每天时间差不多`：周末继续遵守用户填写的做饭时间上限，不要自动放宽。
- `周末反而更忙`：周末不得比工作日更复杂；优先同等或更快、更省事的方案。
- “通常不做饭”的行为必须与上述周末差异规则形成明确、可测试的一致逻辑，不得因为进入周末就完全失效。
- 增加至少 2 个测试，证明“每天时间差不多”和“周末反而更忙”不会在周末突然出现超出约束的复杂餐食。

## 下一阶段任务

当前仍不要进入 Round 04。继续在 `hermes/round-03` 修正上述 3 项。

执行要求：

1. 拉取 `hermes/round-03` 最新版本，不新建分支。
2. 只修正本次复验指出的 3 项，不扩展 Supabase、AI Provider、营养数据库、地图或其他功能。
3. 补充相应自动测试。
4. 运行 `npm run lint`、`npm test`、`npm run build`。
5. 完成一次浏览器 smoke，重点验证：
   - 食堂/外卖餐不会产生错误的家庭采购原料；
   - 便利店成品仍可进入采购清单；
   - 无晚餐时晚间反馈入口仍存在；
   - 周末差异规则真实影响方案。
6. commit + push 到 `hermes/round-03`，更新 PR #3。
7. 更新本交接文档，将结果、测试和最新提交写清楚。
8. 不得合并 `main`，完成后停止并等待 ChatGPT 再次复验。

## 已知问题 / 技术债

- 当前规划引擎仍是有限本地模板池和确定性规则，不是真实 AI，也没有完整营养计算。
- 预算和成本仍为粗略估算，尚未接入个人长期食材环境模型或真实价格。
- profile、周方案、采购清单和每日反馈仍只存在当前浏览器，尚未跨设备同步。
- 重新完成建档会生成新方案并重置当前周采购清单，尚未支持历史周方案。
- 当前 `lint` 实际为 TypeScript `tsc --noEmit`，尚未配置 ESLint。
- `npm install` 报告 2 个既有依赖漏洞（1 moderate、1 high）。

## 执行历史

- 2026-09-01：建立 MVP 基础代码并合并 PR #1。
- 2026-09-01：Round 02 完成核心页面交互；ChatGPT 首轮验收发现日期、UTC 反馈日期键、偏离原因校验问题。
- 2026-09-01：Round 02 修正完成并通过复验，PR #2 合并到 `main`。
- 2026-09-01：ChatGPT 下发 Round 03“本地个性化规划引擎 v0”。
- 2026-09-01：Hermes 完成模板池、确定性规划器、统一 storage、本地个性化闭环并创建 PR #3。
- 2026-09-01：ChatGPT 首次复验 PR #3，发现首页部分餐别空白、替换方案绕过约束、纯素茶叶蛋识别缺口。
- 2026-09-01：Hermes 修正上述 3 项并新增回归测试；最新 CI 已通过。
- 2026-09-01：ChatGPT 第二次完整复验继续发现采购清单外食语义错误、无晚餐时反馈入口消失、周末差异字段未参与规划；PR #3 继续暂缓合并。