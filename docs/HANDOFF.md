# 项目交接文档

## 项目当前状态

- 项目：AI Diet Partner。
- 当前阶段：饮食管理 MVP 原型，Next.js + TypeScript，移动端优先。
- 当前开发分支：`hermes/round-03`；禁止直接修改 `main`。
- 当前 PR：#3 `feat: connect local personalized meal planning`，目标 `main`，OPEN，未合并。
- 当前 PR head：`bec55c55101cc399df50178ac2b6a397c193f24c`。
- GitHub Actions：当前 head CI 已成功。
- Round 03 已实现本地确定性餐食规划、统一 localStorage 数据层、周方案、采购清单与每日反馈闭环。
- 当前仍不接 Supabase、认证、真实 AI Provider 或第三方服务。

## 当前产品边界

继续严格遵守 `AGENTS.md` 与 `docs/PRODUCT.md`：

- MVP 只做饮食管理。
- 前五个大版本禁止加入吉祥物、卡通 IP、拟人角色。
- 不加入喝水、步数、久坐、运动打卡、训练计划等泛健康功能。
- 不做卡路里流水账，不做冰箱库存管理。
- 外卖、食堂、便利店、自己带饭、外食都是正常饮食环境。
- 避免“未达标、还差多少、预计减重”等考核式表达。
- 未经明确任务不得扩展地图、美团/高德、MCP、外部 Agent、真实 AI Provider。

## Round 03 已完成并通过复验的内容

- 用户资料会真实影响本地周方案。
- 厨房能力、做饭时间、工作日/周末差异、外食环境、喜恶/禁忌、预算和重复度参与规划。
- 主方案与替换方案共用当前 profile 的约束过滤。
- 纯素/全素与“对 X 过敏”自然输入已覆盖当前模板池。
- `breakfastPattern` 已参与规划：通常不吃时不生成早餐；有时来不及时优先快速免烹饪早餐。
- “自己带饭”已有独立午餐模板和采购语义。
- 采购清单区分家庭采购、便利店购买和食堂/外卖提供内容。
- 首页部分餐别缺失时不会把其他餐别一起隐藏。
- 无晚餐时晚间反馈入口仍存在。
- 采购勾选、周方案、profile、每日反馈均有本地持久化。

## 当前验证结果

- `npm run lint`：通过。
- `npm test`：33 项通过。
- `npm run build`：通过。
- GitHub Actions：PR 当前 head CI 成功。
- Hermes 已完成浏览器 smoke：建档 → 周方案 → 首页 → 采购 → 刷新持久化 → 每日反馈。
- 第三次复验要求的三项代码提交 `a732825` 已真实存在 GitHub；交接提交为 `bec55c5`。

## ChatGPT 第四次复验：仅剩 1 项合并前语义修正

前三次复验发现的问题已经确认修正。当前不再扩大 Round 03 范围，只修下面这一项，修完通过验证后即可再次交给 ChatGPT 做最终合并判断。

### “通常不吃早餐”时，首页仍把早餐显示成“缺少方案”

规划器现在会根据 `breakfastPattern = 通常不吃` 正确地不生成早餐，这是对的。

但 `components/home-screen.tsx` 目前只看到“当天没有 breakfast”，无法区分：

1. 因用户明确选择“通常不吃早餐”而主动不安排；
2. 因严格约束导致早餐模板真的无法生成。

因此在早晨，用户明明告诉系统自己通常不吃早餐，首页仍会显示：

`早餐暂时没有合适模板，先看其他餐别。`

同时顶部仍显示“先看早餐”。这会把用户主动选择错误地表现成系统失败，也违背“适应用户现实”的产品原则。

要求做最小修正：

- 首页能够知道用户是否选择“通常不吃早餐”。
- 如果是用户主动选择通常不吃早餐：
  - 早晨不要显示“早餐暂时没有合适模板”。
  - 不要继续用“先看早餐”作为顶部提示。
  - 应直接把午餐视为下一顿主要信息；午餐存在时作为早晨的 focus 内容。
  - 不增加“早餐很重要”“建议吃早餐”等健康说教。
- 如果用户本来会吃早餐，但因为条件/禁忌导致早餐确实无可行模板，则继续保留当前轻量 `MissingMeal` 提示。
- 不要为此重构整个首页；读取已保存 profile 或增加一个很轻量的计划状态字段均可，选择更简单可靠的方案。

### 验收

至少增加测试或可验证 smoke，证明：

1. `breakfastPattern = 通常不吃` + 早晨：不出现“早餐暂时没有合适模板”，午餐成为主要下一顿。
2. 正常早餐用户但早餐确实无可行模板时，缺失提示仍能正常出现。
3. `npm run lint`、`npm test`、`npm run build` 全部通过。

## 下一阶段任务

继续使用 `hermes/round-03`，只完成上面的“通常不吃早餐：首页语义”修正。

完成后：

1. commit + push 到 `hermes/round-03`。
2. 更新 PR #3。
3. 更新本文件记录验证结果，并把“下一阶段任务”改为“等待 ChatGPT 最终复验”。
4. 不得合并 `main`。
5. 停止工作并等待 ChatGPT。

## 后续阶段候选（当前不要做）

- `goal`、身高、体重进入正式营养/份量层。
- 每日反馈真正影响下一周方案。
- 历史周方案与跨设备数据层。
- Supabase / 认证。
- 真实 AI Provider。
- 个人食材环境模型与真实价格。

## 执行历史摘要

- PR #1：MVP 基础骨架，已合并。
- PR #2：核心页面交互与日期/反馈修正，已合并。
- PR #3 / Round 03：本地个性化规划和本地数据闭环。
- ChatGPT 前三次复验累计发现并已修复：部分餐别空白、替换方案绕约束、纯素识别、外食采购语义、无晚餐反馈、周末差异、自然语言过敏输入、早餐习惯无效、自己带饭无效。
- 2026-09-01：ChatGPT 第四次复验确认上述问题均已修正，仅保留“通常不吃早餐时首页仍误显示 MissingMeal”这一项合并前修正。
