# 项目交接文档

## 当前状态

- 项目：AI Diet Partner。
- Round 05 / PR #5 已通过 ChatGPT 复验并 squash 合并到 `main`。
- Round 05 合并提交：`61b50e7cd831feecfddc2a9376148af11601211a`。
- PR #5 最终 head：`ae496ee65a4706c3de5e6d05384a31266eb8bac7`；GitHub Actions CI 已通过。
- 云端试用版已部署到 Cloudflare Pages：<https://ai-diet-partner.pages.dev>。
- 当前已经具备首轮 7 天真实自用条件。

## 已验收能力

- 三步建档 → 本周个性化方案 → 采购清单 → 每日反馈 → 生成/更新下周方案闭环可运行。
- 本周/下周方案和采购状态按 `weekStart` 隔离。
- 最新反馈可以重新影响已生成的下周方案。
- 过敏、禁忌、厨房能力、时间和预算仍作为硬约束。
- 云端版本采用 Next.js 静态导出 + Cloudflare Pages，不新增数据库、认证或服务端状态。
- PWA manifest、192/512 PNG 图标、Apple Web App 元数据已补齐；当前不做复杂离线缓存。
- Round 05 本地 lint、51 项测试、build 通过；PR 最终 head CI 通过。
- Hermes 已对公开生产地址完成 390×844 手机尺寸 smoke，覆盖建档、本周、采购持久化、反馈持久化、下周生成/更新和移动端布局。

## 首轮试用地址

<https://ai-diet-partner.pages.dev>

试用记录：`docs/TRIAL-01.md`

重要限制：本轮所有用户数据仍只保存在当前浏览器 localStorage。不要使用无痕模式；清除浏览器数据或更换设备会丢失试用数据。

## 当前产品边界

继续遵守 `AGENTS.md` 与 `docs/PRODUCT.md`：

- MVP 只做饮食管理。
- 前五个大版本禁止加入吉祥物、卡通 IP、拟人角色。
- 不加入喝水、步数、久坐、运动打卡、训练计划等泛健康功能。
- 不做卡路里流水账，不做冰箱库存管理。
- 外卖、食堂、便利店、自己带饭、外食都是正常饮食环境。
- 避免考核式、责备式表达。
- 当前不接 Supabase、认证、真实 AI Provider、地图/MCP/外部 Agent。

## 下一阶段任务

**暂停继续开发，开始 TRIAL-01 首轮 7 天真实自用。**

试用期间：

1. 使用同一台手机、同一个浏览器访问生产地址。
2. 按 `docs/TRIAL-01.md` 记录真实问题，只记录会影响使用或执行的问题。
3. 问题分为：阻断 / 明显影响 / 可以后优化。
4. 若出现“阻断”问题，可立即停止试用并交给 ChatGPT 复盘，不必等满 7 天。
5. 非阻断问题先记录，不在试用中途持续改代码，避免版本变化污染测试结果。
6. Day 7 重点验证：根据一周反馈生成/更新的下周方案是否真正更符合现实。

试用结束后，由 ChatGPT 汇总 TRIAL-01 结果，再决定下一轮产品修正范围。当前不要自行进入 Round 06。

## 后续候选（试用期间不要做）

- 根据 7 天真实问题进行产品修正。
- 正式营养/份量层（goal、年龄、身高、体重）。
- Supabase / 认证 / 云同步。
- 真实 AI Provider。
- 个人食材环境模型与真实价格。
