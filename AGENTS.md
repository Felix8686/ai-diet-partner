# AI Diet Partner — 开发约束

这份文件给 Codex、Claude Code、Gemini CLI、Hermes 等编码 Agent 使用。任何实现都必须先遵守这些产品边界。

## 第一原则

如果一个功能不能直接帮助用户解决以下三个问题之一，就不要加入 MVP：

1. 这一顿怎么吃？
2. 这周怎么买？
3. 为什么方案要调整？

## 禁止自行扩展

- 不加入步数、喝水、久坐、睡眠等泛健康提醒。
- 不加入卡路里逐口记录。
- 不做冰箱库存系统。
- 不接地图、餐厅营养库、全国商品库。
- 前五个大版本禁止加入吉祥物、卡通 IP、拟人助手形象。
- 不把外食视为失败或异常。
- 不使用“未达标”“还差多少”“预计减重 X kg”等具有考核感的默认文案。

## UX

- 首页只回答“接下来怎么吃”。
- 反馈只问执行情况和偏离原因，不做道德评价。
- 建档保持 3 步轻量流程；非关键项允许稍后补。
- 本周方案使用横向日期选择 + 单日详情，不使用 7 列表格。
- 采购清单只有已买/未买，不衍生库存管理。
- 辅助提示尽量用小字，不用大卡片占据主视觉。

## 工程策略

- 当前先用 mock data 跑通前端闭环。
- 数据层保持可切换，后续接 Supabase/PostgreSQL。
- AI 调用必须放在服务端，Provider 可替换，不把模型厂商写死进页面组件。
- 规则过滤与大模型生成分层：预算、厨具、时间、忌口等硬约束必须由规则层再次校验。
- 新功能默认先提 Issue，不要顺手加入。
